# Copy to .env for local development. Use unique secrets in each deployment.
NODE_ENV=development
SESSION_SECRET=replace-with-a-long-random-secret
# Comma-separated browser origins allowed to call the API.
CORS_ORIGIN=http://localhost:3000
# Used only the first time the default super-admin account is seeded.
SUPERADMIN_PASSWORD=replace-immediately
